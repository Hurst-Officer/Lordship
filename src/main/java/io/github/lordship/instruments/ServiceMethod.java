package io.github.lordship.instruments;

public enum ServiceMethod {
    PERSONAL, MAIL, POST_AND_MAIL, EMAIL, OTHER
    // How the document reached the tenant. Recorded by the property manager,
    // and half of what instrument_served_has_facts requires.
}
