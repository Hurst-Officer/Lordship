package io.github.lordship.homes;

// What the structure physically is, which does not change. What it is used for
// does change, and is deliberately not here -- that belongs to lot tagging.
public enum StructureType {
    APARTMENT,
    SINGLE_FAMILY,
    GARAGE,
    SHOP,
    OUTBUILDING
}